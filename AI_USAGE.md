# AI Usage Documentation

本项目在开发过程中使用了 AI 编程工具，以下记录使用方式、协作边界和验证策略。

## 使用了哪些 AI 工具

- **Claude Code (Claude Opus 4)** — 主要工具，用于代码生成、测试编写、文档撰写
- 使用方式：通过 CLI 交互，逐步提出需求并审查输出

## AI 主要帮助完成了哪些部分

| 部分 | AI 参与度 | 说明 |
|------|-----------|------|
| 项目骨架搭建 | 高 | pyproject.toml、目录结构、CLI 注册、Pydantic 模型定义 |
| 核心算法实现 | 中 | 基于人工设计的公式和规则，AI 完成代码编写 |
| 示例数据生成 | 高 | 3 个场景的 traffic/telemetry 数据由 AI 脚本生成 |
| 测试用例 | 高 | 59 个测试由 AI 生成，人工审查边界条件和断言正确性 |
| 报告生成器 | 高 | Markdown 报告模板和格式化逻辑 |
| 文档 | 中 | AI 生成初稿，人工审核结构和技术准确性 |

## 一个被采纳的关键建议

**Profile Lookup 的多级降级策略。**

AI 建议将 profile 查询设计为四级 fallback（精确匹配 → TP 插值 → 跨 GPU 缩放 → 保守默认），每级附带递减的置信度分数。这个设计被采纳的原因：

- 实际部署中不可能为所有 GPU × 后端 × 精度 × TP 组合都准备 profile
- 显式置信度让下游评分器能区分「可靠估算」和「粗略猜测」
- 最终报告中低置信度方案会附带更强的验证建议，形成完整信息链

## 一个被拒绝或修正的 AI 建议

**Prefix Cache 的启用判断。**

AI 最初实现为 `prefix_reuse_rate > 0.3` 即启用 prefix cache。这个判断有缺陷：

- 场景二（长文本生成）中只有 9% 的请求携带 prefix，但这 28 个请求之间的复用率是 50%
- 按原始逻辑会启用 prefix cache，但实际上 91% 的请求完全不受益，白白增加显存开销

修正后的逻辑要求**同时满足**：
1. prefix 覆盖率 > 30%（足够多的请求能受益）
2. prefix 复用率 > 30%（缓存命中率够高）

这是典型的「AI 生成了逻辑正确但语义错误的代码」— 形式上满足需求描述，但在边缘场景下决策不合理。

## 如何验证 AI 生成的代码

1. **单元测试覆盖核心公式** — 显存估算、KV cache 计算、评分函数均有参数化测试，验证数值正确性
2. **端到端场景测试** — 3 个不同特征的场景，验证决策逻辑在不同 workload 下的行为差异
3. **人工审查决策输出** — 检查 `decision_report.md` 的推理是否自洽（如：prefix 复用率 99% 时确实启用了 cache）
4. **对抗性测试** — 用极端输入（空数据、单条记录、全部相同 prefix）验证不崩溃且行为合理
5. **回归验证** — 修改 prefix cache 逻辑后重新跑全部场景，确认场景一仍启用、场景二已关闭

测试覆盖率 93%（不含 CLI 入口层）。

## 哪些关键技术判断由人独立完成

1. **显存估算公式的推导** — KV cache 按 `2 × layers × kv_heads × head_dim × dtype_bytes / TP` 计算，这需要理解 GQA 架构下 KV head 的分布方式和 TP 切分语义

2. **TP 必须整除 num_kv_heads 的约束** — 不是所有 TP 值都合法，GQA 模型中 KV head 数通常远小于 attention head 数，TP=3 或 TP=5 几乎不可能可行

3. **缩容比扩容需要更多确认窗口（5 vs 3）** — 来自实际运维经验：误缩容导致 SLO 违反的恢复时间远长于误扩容浪费的成本

4. **Prefix Cache 启用条件需要「覆盖率 × 复用率」双重检查** — 在审查 AI 输出时发现单一阈值的缺陷并修正

5. **评分函数中成本和延迟的权重分配** — minimize_cost 场景下成本权重 0.5，maximize_goodput 场景下延迟权重 0.4，这些比例基于对业务优先级的理解

6. **Pipeline Parallel 对延迟的影响判断** — PP 引入 pipeline bubble（约 (PP-1)/PP 的效率损失），在大多数在线服务场景中 PP=1 是最优选择，除非单卡装不下模型
